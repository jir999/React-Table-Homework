import React, { useState } from "react";
import Row from "../Row";

const Column = () => {
  const [columnData, setColumnData] = useState(null);

  return <div></div>;
};

export default Column;
